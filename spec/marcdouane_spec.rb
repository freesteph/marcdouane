# frozen_string_literal: true

RSpec.describe Marcdouane do
  it "has a version number" do
    expect(Marcdouane::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
